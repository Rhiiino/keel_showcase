# Intelligence hub icons

Section icons for the Intelligence menu hub cards.

## Files

Place one image per section (PNG, SVG, or WebP):

- `models.png` — Models card
- `tools.png` — Tools card

Recommended size: 3:2 landscape (e.g. 384×256). When a file is missing, inline SVG fallbacks are shown.
