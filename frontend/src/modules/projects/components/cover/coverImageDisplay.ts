// keel_web/src/modules/projects/components/cover/coverImageDisplay.ts

// Shared display classes for framed project cover images (form preview + Kanban cards).

/** Fit the full image in the frame; zoom/pan crops only when scale is above 1. */
export const PROJECT_COVER_IMAGE_CLASS = "h-full w-full object-contain";

export const PROJECT_COVER_IMAGE_FRAME_CLASS =
  "h-full w-full bg-stone-950/90";
